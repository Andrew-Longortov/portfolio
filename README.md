# Портфолио — стартовый скелет

Это только структура файлов и папок с комментариями-подсказками (TODO),
без готовой логики и стилей — код пишешь сам, по шагам из
`portfolio-project-plan.md`.

## Как запустить локально

Просто открой `index.html` в браузере, или, если стоит VS Code с
расширением Live Server — правый клик по `index.html` → "Open with
Live Server" (удобнее: страница будет обновляться при сохранении).

## Порядок работы

Смотри `portfolio-project-plan.md` (пришёл вместе со скелетом) —
там расписаны все milestone'ы по порядку: HTML → CSS → JS-проекты →
деплой. Общий roadmap обучения — в `roadmap.md`.
