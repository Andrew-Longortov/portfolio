// MILESTONE 3: Калькулятор
//
// Вопросы, на которые тебе нужно ответить своим кодом:
// 1. Где хранить текущий ввод и предыдущее значение/оператор?
//    (переменные состояния, а не чтение прямо из DOM на каждый чих)
// 2. Как обрабатывать нажатие цифры, оператора, "=", "C" — один
//    обработчик на весь блок кнопок (делегирование событий) или
//    отдельные обработчики?
// 3. Что делать при делении на ноль?
// 4. Как обрабатывать цепочку операций типа 5 + 3 - 2 = ?
//
// Готовых решений здесь нет специально — это и есть часть обучения.
// Когда что-то напишешь и застрянешь — пришли код или текст ошибки.

const display = document.getElementById("display");

function appendToDisplay(input){
    display.value += input;
}

function clearDisplay(){
    display.value = "";
}

function calculate(){
    try {
        display.value = eval(display.value);
    }
    catch(error) {
        display.value = "Error";
    }
}